# Discord Bot Token (from Discord Developer Portal)
DISCORD_TOKEN=your_bot_token_here

# Optional: Channel ID to post "Bot has reloaded" on every startup (e.g. after deploy).
# Right-click channel → Copy ID (enable Developer Mode in Discord). Works even when !setchannel hasn't been run.
# RELOAD_NOTIFY_CHANNEL_ID=1234567890123456789

# Optional: Not needed—bot syncs commands to all servers it's in
# DISCORD_GUILD_ID=your_guild_id_here
